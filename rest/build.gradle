apply plugin: 'application'

mainClassName = 'com.kevinfan.coolj.rest.dropwizard.hello.HelloApplication'

run {
    args 'server', 'hello.yml'
}

dependencies {
    compile libs.dropwizard_core
    compile libs.dropwizard_assets
}
